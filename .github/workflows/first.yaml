# This is a basic workflow to help you get started with Actions
# name: My First Workflow
# on: push
# jobs:
#   first-job:
#     runs-on: ubuntu-latest
#     steps:
#       - name: Print Hello
#         run: echo "Hello Sarthak"

# This is a basic workflow, and is meant to help you get started with Actions. You can delete this file, and start from scratch, or you can edit it to fit your needs. The workflow will run on every push to the repository.
# name: Fixed Workflow
# on: push
# jobs:
#   first-job:
#     runs-on: ubuntu-latest
#     steps:
#       - name: Print Hello
#         run: echo "Hello Sarthak"
#       - name: Show Date
#         run: date
#       - name: Show Files
#         run: ls

# This workflow will checkout the repository, show the files in the repository, and show the current folder. You can modify this workflow to fit your needs.
# name: Checkout Example
# on: push
# jobs:
#   checkout-job:
#     runs-on: ubuntu-latest
#     steps:
#       - name: Download Repository
#         uses: actions/checkout@v4
#       - name: Show Files
#         run: ls
#       - name: Show Current Folder
#         run: pwd


# This workflow will install a programming language of your choice. You can modify this workflow to fit your needs.
# name: Node Example
# on: push
# jobs:
#   node-job:
#     runs-on: ubuntu-latest
#     steps:
#       - name: Checkout Repository
#         uses: actions/checkout@v4
#       - name: Install Node.js
#         uses: actions/setup-node@v4 #install node.js
#         with:
#           node-version: 18 #specify the version of node.js you want to install
#       - name: Check Node.js Version
#         run: node -v
#       - name: Check npm Version
#         run: npm -v

# This workflow will run the start script defined in your package.json file. You can modify this workflow to fit your needs.
# name: Node CI
# on: push
# jobs:
#   build:
#     runs-on: ubuntu-latest
#     steps:
#       - name: Checkout Repository
#         uses: actions/checkout@v4
#       - name: Setup Node.js
#         uses: actions/setup-node@v4
#         with:
#           node-version: 18
#       - name: Install Dependencies
#         run: npm install #npm init -y to create package.json file and npm install to install dependencies
#       - name: Run Application
#         run: npm start

# This workflow will run the start script defined in your package.json file and also cache the npm dependencies to speed up the workflow. You can modify this workflow to fit your needs.
# name: Node CI
# on: push
# jobs:
#   build:
#     runs-on: ubuntu-latest
#     steps:
#       - name: Checkout Repository
#         uses: actions/checkout@v4
#       - name: Setup Node.js
#         uses: actions/setup-node@v4
#         with:
#           node-version: 18
#           cache: 'npm'
#       - name: Install Dependencies
#         run: npm install #npm install is used to install the dependencies defined in the package-lock.json file
#       - name: Run Application
#         run: npm start

# This workflow will run multiple jobs in parallel. You can modify this workflow to fit your needs.
# name: Multiple Jobs Workflow
# on: push
# jobs:
#   build:
#     runs-on: ubuntu-latest
#     steps:
#       - name: Checkout Repository
#         uses: actions/checkout@v4
#       - name: Print Build Message
#         run: echo "This is the build job"
#   test:
#     runs-on: ubuntu-latest
#     steps:
#       - name: Print Test Message
#         run: echo "This is the test job"

# This workflow will run multiple jobs in parallel and also have a job that depends on the other two jobs. You can modify this workflow to fit your needs.
# name: Job Dependency Workflow
# on: push
# jobs:
#   build:
#     runs-on: ubuntu-latest
#     steps:
#       - name: Build Step
#         run: echo "Building Application"
#   test:
#     needs: build # This means that the test job will only run after the build job has completed successfully.
#     runs-on: ubuntu-latest
#     steps:
#       - name: Test Step
#         run: echo "Testing Application"
#   deploy:
#     needs: test # This means that the deploy job will only run after both the build and test jobs have completed successfully.
#     runs-on: ubuntu-latest
#     steps:
#       - name: Deploy Step
#         run: echo "Deploying Application"

# This workflow will demonstrate how to use environment variables in GitHub Actions. You can modify this workflow to fit your needs.
# name: Environment Variables Workflow
# on: push
# jobs:
#   demo:
#     runs-on: ubuntu-latest
#     env: # Define environment variables that will be available to all steps in this job
#       GREETING: "Hello"
#       NAME: "Sarthak"
#     steps:
#       - name: Print Greeting
#         run: echo "$GREETING, $NAME!" # Use the environment variables defined above
#       - name: Show Environment Variables
#         run: env # This will print all environment variables available in this step, including GREETING and NAME

# This workflow will demonstrate how to use secrets in GitHub Actions. You can modify this workflow to fit your needs. Make sure to add a secret named MY_SECRET in your repository settings before running this workflow.
# name: Secrets Workflow
# on: push
# jobs:
#   secret-job:
#     runs-on: ubuntu-latest
#     steps:
#       - name: Print Secret
#         run: echo ${{ secrets.MY_SECRET }} # Use the secret defined in the repository settings. Make sure to add a secret named MY_SECRET in your repository settings before running this workflow.

# This workflow will demonstrate how to build a Docker image using GitHub Actions. You can modify this workflow to fit your needs. Make sure to have a Dockerfile in the root of your repository before running this workflow.
# name: Docker Build
# on: push
# jobs:
#   docker-job:
#     runs-on: ubuntu-latest
#     steps:
#       - name: Checkout Repository
#         uses: actions/checkout@v4
#       - name: Build Docker Image
#         run: docker build -t my-node-app . # then docker run my-app to run the container
#     Inside Dockerfile:

# CMD ["npm", "start"]

# Meaning:

# When container starts:
# run npm start

# Then again:

# npm start
#    ↓
# node index.js
#    ↓
# console.log()  

# This workflow will demonstrate how to build and run a Docker image using GitHub Actions. You can modify this workflow to fit your needs. Make sure to have a Dockerfile in the root of your repository before running this workflow.
# name: Python Docker Build
# on: push
# jobs:
#   docker-build:
#     runs-on: ubuntu-latest
#     steps:
#       - name: Checkout Repository
#         uses: actions/checkout@v4
#       - name: Build Docker Image
#         run: docker build -t my-python-app . # then docker run my-python-app to run the container
#       - name: Run Docker Container
#         run: docker run my-python-app

# This workflow will demonstrate how to build and push a Docker image to Docker Hub using GitHub Actions. You can modify this workflow to fit your needs. Make sure to have a Dockerfile in the root of your repository before running this workflow, and also add your Docker Hub username and password as secrets in your repository settings.
# name: Docker Push CI
# on: push
# jobs:
#   docker:
#     runs-on: ubuntu-latest
#     steps:
#       - name: Checkout Repository
#         uses: actions/checkout@v4
#       - name: Login to Docker Hub
#         uses: docker/login-action@v3 # This action is used to log in to Docker Hub using the provided credentials
#         with:
#           username: ${{ secrets.DOCKER_USERNAME }} # Add your Docker Hub username as a secret in your repository settings
#           password: ${{ secrets.DOCKER_PASSWORD }} # Add your Docker Hub password as a secret in your repository settings
#       - name: Build Docker Image
#         run: docker build -t ${{ secrets.DOCKER_USERNAME }}/python-app . # Build the Docker image and tag it with your Docker Hub username
#       - name: Push Docker Image
#         run: docker push ${{ secrets.DOCKER_USERNAME }}/python-app # Push the Docker image to Docker Hub using the tag specified in the previous step

# This workflow will demonstrate how to build and push a Docker image for a Java application to Docker Hub using GitHub Actions. You can modify this workflow to fit your needs. Make sure to have a Dockerfile in the root of your repository before running this workflow, and also add your Docker Hub username and password as secrets in your repository settings.
# name: Java Docker Push
# on: push
# jobs:
#   docker-build:
#     runs-on: ubuntu-latest
#     steps:
#       - name: Checkout Repository
#         uses: actions/checkout@v4
#       - name: Login to Docker Hub
#         uses: docker/login-action@v3
#         with:
#           username: ${{ secrets.DOCKER_USERNAME }}
#           password: ${{ secrets.DOCKER_PASSWORD }}
#       - name: Build Docker Image
#         run: docker build -t ${{ secrets.DOCKER_USERNAME }}/java-app . # Build the Docker image and tag it with your Docker Hub username
#       - name: Push Docker Image
#         run: docker push ${{ secrets.DOCKER_USERNAME }}/java-app # Push the Docker image to Docker Hub using the tag specified in the previous step

# This workflow will demonstrate how to build and run a Docker image for a Python application using GitHub Actions. You can modify this workflow to fit your needs. Make sure to have a Dockerfile in the root of your repository before running this workflow.
# name: Python CI-CD
# on: push
# jobs:
#   build:
#     runs-on: ubuntu-latest
#     steps:
#       - name: Checkout Code
#         uses: actions/checkout@v4
#       - name: Build Docker Image
#         run: docker build -t my-docker-app . # Build the Docker image and tag it as my-docker-app
#       - name: Run Docker Container
#         run: docker run -d -p 5000:5000 my-docker-app # Run the Docker container in detached mode and map port 5000 of the container to port 5000 of the host machine

# This workflow will demonstrate how to run a workflow on a self-hosted runner. You can modify this workflow to fit your needs. Make sure to set up a self-hosted runner in your repository settings before running this workflow.
# name: Self Hosted Demo
# on: push
# jobs:
#   demo:
#     runs-on: self-hosted # This means that the workflow will run on a self-hosted runner instead of a GitHub-hosted runner. Make sure to set up a self-hosted runner in your repository settings before running this workflow.
#     steps:
#       - name: Checkout Repository
#         uses: actions/checkout@v4
#       - name: Print Message
#         run: echo "This workflow is running on a self-hosted runner!"
# STEP 1 — Start Runner First

# Go to runner folder:

# cd actions-runner

# Then run:

# .\run.cmd

# When you see:

# Listening for Jobs

# your runner is active.

# This workflow will demonstrate how to build, push, and run a Docker image for a Java application using GitHub Actions on a self-hosted runner. You can modify this workflow to fit your needs. Make sure to have a Dockerfile in the root of your repository before running this workflow, and also add your Docker Hub username and password as secrets in your repository settings.
# name: Java CI/CD Pipeline
# on: push
# jobs:
#   build-and-push:
#     runs-on: self-hosted
#     steps:
#       - name: Checkout Repository
#         uses: actions/checkout@v4
#       - name: Login to Docker Hub
#         uses: docker/login-action@v3
#         with:
#           username: ${{ secrets.DOCKER_USERNAME }}
#           password: ${{ secrets.DOCKER_PASSWORD }}
#       - name: Build Docker Image
#         run : docker build -t ${{ secrets.DOCKER_USERNAME }}/quote-app . # Build the Docker image and tag it with your Docker Hub username
#       - name: Push Docker Image
#         run: docker push ${{ secrets.DOCKER_USERNAME }}/quote-app # Push the Docker image to Docker Hub using the tag specified in the previous step
#       - name: Run Docker Container
#         run: docker run ${{ secrets.DOCKER_USERNAME }}/quote-app # Run the Docker container using the image built in the previous step

# This workflow will demonstrate how to trigger a workflow manually from the GitHub Actions tab in the repository. You can modify this workflow to fit your needs. This workflow will only run when manually triggered, and will not run on any push or pull request events.
# name: Manual Workflow Demo
# on:
#   workflow_dispatch: # This means that the workflow will only run when manually triggered from the GitHub Actions tab in the repository.
# jobs:
#   demo:
#     runs-on: ubuntu-latest
#     steps:
#       - name: Checkout Repository
#         uses: actions/checkout@v4
#       - name: Print Message
#         run: echo "This workflow was triggered manually!"

# name: Manual Workflow Practice
# on:
#   workflow_dispatch:
# jobs:
#   practice:
#     runs-on: ubuntu-latest
#     steps:
#       - name: Checkout Repository
#         uses: actions/checkout@v4
#       - name: Print Message
#         run: echo "This is for practice manual triggering!"