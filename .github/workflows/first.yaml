# name: My First Workflow
# on: push
# jobs:
#   first-job:
#     runs-on: ubuntu-latest
#     steps:
#       - name: Print Hello
#         run: echo "Hello Sarthak"

name: Fixed Workflow
on: push
jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: Print Hello
        run: echo "Hello Sarthak"
      - name: Show Date
        run: date
      - name: Show Files
        run: ls